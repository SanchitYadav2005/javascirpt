const form = document.querySelector("#shelterForm")
form.addEventListener('submit',function(e){
    e.preventDefault(); // it is going to stop the default thing that a form does.
    console.log("fuck")// sorry for the fuck.
})